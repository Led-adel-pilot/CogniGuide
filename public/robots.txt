User-agent: *
# Allow site publicly
Allow: /

# Block private/internal endpoints
Disallow: /dashboard
Disallow: /api/
Disallow: /_next/
Allow: /manifest.json
Disallow: /*.json$

# (OPTIONAL) Avoid crawling query-string URLs — use only if intentional
# Disallow: /*?*

# Ensure sitemap is discoverable
Sitemap: https://www.cogniguide.app/sitemap.xml
